/**
 * QR landing pages sit outside the marketing SiteShell (no main nav / footer).
 * Each page wraps itself in {@link QrShell}.
 */
export default function QrLayout({ children }: { children: React.ReactNode }) {
  return children;
}
